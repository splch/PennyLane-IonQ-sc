version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.11"
  apt_packages:
    - graphviz

sphinx:
  configuration: doc/conf.py
  fail_on_warning: false

python:
  install:
    - requirements: doc/requirements.txt
    - method: pip
      path: .

formats:
  - htmlzip
